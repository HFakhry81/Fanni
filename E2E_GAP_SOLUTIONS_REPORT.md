# Fanni — تقرير حلول سد الثغرات والفجوات الهندسية

**التاريخ:** 5 سبتمبر 2026  
**المرجع:** تحليل الثغرات (مالي · جغرافي · نزاعات · أمان) + وثائق الموديولات النهائية + حالة الكود في `main`  
**الهدف:** أفضل الحلول المتّبعة لكل ثغرة، وما تم تنفيذه فعليًا في المستودع، وما يبقى مفتوحًا.

---

## 0) الخلاصة التنفيذية

| المجال | الحالة العامة في الكود | الأولوية المتبقية |
|--------|------------------------|-------------------|
| سباق قبول الطلب / خصم النقاط | **مسدود بقوة** (`FOR UPDATE` + transaction) | اختبارات حمل متزامنة |
| شحن يدوي / مرجع مكرر | **جزئي** (اعتماد يدوي بلا uniqueness على المرجع) | قيد فريد + إثبات دفع |
| تصنيف النقاط (ليست نقدًا) | **موجود في الشروط والواجهة** | توثيق قانوني/محاسبي رسمي |
| تسريب هاتف في الوصف | **جزئي** (تحذير UI فقط) | فلتر سيرفر + رفض/تنبيه |
| GPS spoofing | **جزئي** (محافظة + tiers مسافة) | تحقق خادم أقوى + سقف |
| نزاع / عميل غير مستجيب | **موجود** (auto-dispute + refund rules) | مراقبة إساءة الاستخدام |
| تهرّب العمولة بعد الكشف | **جزئي** (فني لا يلغي؛ الاسترداد مشروط بعدم اتصال) | سياسة إلغاء بعد الكشف |
| أدمن افتراضي | **موجود** (`mustChangePassword` + بانر) | فرض تغيير قبل أي صلاحية |
| Rate limit على Login/OTP | **موجود** | مراقبة Redis/توزيع في الإنتاج |

---

## 1) النظام المالي والمحفظة

### 1.1 سباق العمليات (Race على القبول/الخصم)

**أفضل حل متّبع**
1. معاملة DB واحدة: قفل الصف `orders … FOR UPDATE` ثم `wallets … FOR UPDATE`.
2. تعيين الطلب شرطيًا: `UPDATE … WHERE status = 'pending'` — إن فشل → `ORDER_UNAVAILABLE` / 409.
3. خصم ذري للنقاط داخل نفس المعاملة؛ رفض إن الرصيد غير كافٍ.
4. اختبارات تزامنية (N فنيين × نفس `orderId`) تتوقع قبولًا واحدًا فقط.

**ما تم في Fanni**
- `unlockLeadAtomically` في `artifacts/api-server/src/lib/leadUnlock.ts`:
  - `SELECT … FROM orders … FOR UPDATE`
  - رفض إن `status !== pending`
  - `SELECT … FROM wallets … FOR UPDATE` ثم خصم
  - تعيين الطلب بـ `WHERE status = 'pending'`
- مسار القبول يعيد 409 عند «Order already accepted by another technician».
- E2E منطق: قبول مزدوج متوقع كتعارض (`e2e/tests/logic/30-edge-conditions` B).

**متبقي**
- اختبار تحميل/تزامن Vitest أو k6 على `accept` وليس فقط تسلسلي Playwright.
- مراجعة أن كل مسارات unlock تمر عبر نفس الدالة فقط.

---

### 1.2 احتيال طلبات التحويل اليدوية (`payment_requests`)

**أفضل حل متّبع**
1. **Unique constraint** على `(payment_method, reference_number)` للحالات غير الفارغة (أو hash المرجع).
2. ربط المبلغ بالنقاط من **باقة النظام** فقط (منع تزوير `pointsRequested`).
3. إثبات دفع إلزامي (صورة إيصال) + مراجعة أدمن ذات صلاحية `manage_payments`.
4. سجل تدقيق (`admin_audit`) لكل تأكيد/رفض.
5. لاحقًا: بوابة دفع (OPay/…) بدل الاعتماد البصري فقط.

**ما تم في Fanni**
- إنشاء الطلب يفرض المبلغ/النقاط من الباقة عند وجود `packageId`.
- تأكيد/رفض أدمن عبر API + إشعارات.
- شروط الاستخدام توضّح أن النقاط ليست سحبًا نقديًا.
- **لا يوجد** قيد uniqueness على `reference_number` في schema الحالي (`008_payment_requests.sql`).

**متبقي (موصى به فورًا)**
```text
- Migration: UNIQUE INDEX على reference_number حيث NOT NULL / غير فارغ
- رفض API إن المرجع مكرر أو قصير/ضعيف
- إرفاق إيصال إلزامي قبل ظهور الطلب في قائمة الأدمن
- لوحة أدمن: تحذير عند تطابق المرجع مع طلب سابق
```

---

### 1.3 الامتثال / تصنيف الأرصدة (ليست نقدًا قابلًا للسحب)

**أفضل حل متّبع**
1. تصنيف محاسبي صريح: **نقاط استهلاك خدمة / رصيد استخدام** — غير قابلة للسحب النقدي.
2. شروط استخدام فني ملزمة عند التسجيل + إقرار في التطبيق.
3. دفتر أستاذ يفصل: Cash-in من التحويلات ↔ Points liability (وليس ودائع عملاء قابلة للاسترداد النقدي).
4. استرداد النقاط فقط ضمن سياسة نزاع/إلغاء مبكر — وليس «سحب كاش».

**ما تم في Fanni**
- نصوص الشروط في `AppContext` (`terms.summaryTech` / `terms.acceptTech`): النقاط ليست نقودًا ولا سحب ولا تحويل.
- Welcome/admin bonus كنقاط ترويجية داخل المحفظة.
- نزاعات الاسترداد عبر `dispute_refund` وليس تحويل بنكي للفني.

**متبقي**
- مراجعة قانونية خارجية لصياغة الشروط والحسابات.
- توثيق GL: ربط كل تأكيد دفع بقيود يومية واضحة في وثيقة الحسابات النهائية.

---

## 2) المطابقة الجغرافية والطلبات

### 2.1 تسريب بيانات التواصل في وصف المشكلة (Lead Leakage)

**أفضل حل متّبع**
1. تحذير UI واضح (موجود جزئيًا).
2. **فلتر سيرفر** على `problemDescription`: كشف أنماط هاتف مصري / روابط واتساب → رفض أو تنبيه + تخزين للتنبيه.
3. الإبقاء على الهاتف في حقل مخصص فقط؛ إخفاؤه قبل unlock (`maskPhoneDisplay`).
4. مراجعة أدمن لطلبات «مشبوهة».

**ما تم في Fanni**
- إخفاء الهاتف قبل الفتح: `maskPhoneDisplay` / `maskSensitiveOrderFields`.
- تحذير في `new-order.tsx` بعدم كتابة أرقام في الوصف.
- **لا يوجد** فلتر رفض سيرفر على نص الوصف حتى الآن.

**متبقي**
```text
- validateOrderDescriptionPII() على POST /api/orders
- اختبارات وحدة لأنماط 01xxxxxxxxx / wa.me / @
- سياسة: إن وُجد هاتف في الوصف → 400 أو flag للأدمن
```

---

### 2.2 التلاعب بإحداثيات الفني (GPS Spoofing)

**أفضل حل متّبع**
1. مطابقة أساسية بالمحافظة (hard gate) + تفضيل المسافة (soft).
2. بث متدرج بمسافات محدودة (tiers) مع **سقف أقصى**.
3. اختياريًا: رفض تحديث موقع إن القفزة غير فيزيائية (سرعة مستحيلة).
4. عدم الاعتماد على GPS الجهاز وحده لقرار «وصول» — تأكيد عميل / وقت / مسافة للطلب.

**ما تم في Fanni**
- `orderBroadcaster`: بوابة مهنة + محافظة؛ المسافة عبر tiers افتراضيًا `15 → 50 → 100` كم (قابلة للضبط بـ env).
- تأكيد وصول العميل (`confirm-arrival`) ومسار `start` منفصلان.
- لا يوجد بعد: كشف spoofing بحدود سرعة / إثبات موقع طرف ثالث.

**متبقي**
- فرض `BROADCAST_RADIUS_TIERS_KM` في الإنتاج مع سقف ≤ 100 كم (أو أقل حسب السياسة).
- تسجيل موقع الفني عند القبول والوصول للمراجعة.

---

### 2.3 توسع الدائرة بلا حدود

**أفضل حل متّبع**
- قائمة tiers ثابتة + حد زمني بين المستويات + عدم تجاوز المحافظة إلا بقرار منتج صريح.

**ما تم**
- Tiers محدودة في الكود؛ المحافظة تبقى شرطًا صلبًا في المطابقة.
- يمكن لمشغّل env توسيع القائمة خطأً — يحتاج توثيق تشغيل.

---

## 3) النزاعات والإلغاء

### 3.1 عميل وهمي / غير مستجيب بعد فتح البيانات

**أفضل حل متّبع**
1. إخفاء البيانات حتى تأكيد الخصم.
2. أسباب فشل خدمة محددة؛ `client_not_present` / `client_refused` → طلب استرداد + نزاع.
3. استرداد تلقائي محدود (سبب مؤهل + لم يُضغط اتصال/واتساب + سقف يومي).
4. مراجعة أدمن لباقي الحالات.

**ما تم في Fanni**
- Unlock بعد التأكيد؛ هاتف مقنّع قبلها.
- `fail-service` + نزاع؛ `autoDispute` للأسباب المؤهلة مع `DISPUTE_AUTO_DAILY_CAP` (افتراضي 2).
- استرداد مبكر عند إلغاء عميل وفق شروط unlock (عدم اتصال).
- E2E منطق: مسارات عدم الوصول / رفض العميل / حلول بديلة بدون استرداد تلقائي.

**متبقي**
- لوحة مؤشرات churn / نسبة النزاعات لكل فني.
- سياسة أوضح للعميل المتكرر الإلغاء.

---

### 3.2 إساءة «إلغاء بعد الكشف» للتهرب من العمولة

**أفضل حل متّبع**
1. الفني **لا يلغي** الطلب بعد القبول (موجود).
2. الاسترداد غير متاح إن تم `clickedCall` / WhatsApp (موجود في منطق النزاع).
3. عمولة الكشف غير قابلة للاسترداد إلا بمسار نزاع معتمد.
4. كشف أنماط: فتح → اتصال → إكمال خارج المنصة (إشارات سلوكية لاحقًا).

**ما تم**
- `Technicians cannot cancel orders`.
- عميل يلغي فقط وهو `pending` (ذرّي).
- تتبع `clickedCall` / `clickedWhatsapp` يمنع الاسترداد التلقائي.

**متبقي**
- إن وُجد زر «إلغاء قبول» في الواجهة القديمة: إزالته أو تحويله لنزاع فقط.
- تنبيه للعميل إن تواصل خارج المنصة (تعليمي).

---

## 4) الأمان والتحقق

### 4.1 حساب أدمن افتراضي

**أفضل حل متّبع**
1. عدم إنشاء أدمن افتراضي في الإنتاج إلا بـ flag صريح لأول تثبيت.
2. `mustChangePassword=true` + بانر + منع العمليات الحساسة حتى التغيير.
3. تدوير كلمة المرور فور النشر؛ تعطيل `admin/admin` في الوثائق العامة.

**ما تم في Fanni**
- تخطي seed الأدمن في الإنتاج بدون `FANNI_ALLOW_PROD_ADMIN_SEED`.
- تعليم `mustChangePassword` إن بقيت كلمة `admin`.
- بانر في واجهة الأدمن (`mustChangePassword`).
- Rate limit على `change-password`.

**متبقي**
- فرض middleware: رفض APIs الإدارية إن `mustChangePassword` ما زال true (أقوى من بانر فقط).

---

### 4.2 Rate limiting على OTP و Login

**أفضل حل متّبع**
- حد لكل IP ولكل معرّف؛ نوافذ زمنية؛ 429 واضح؛ في الإنتاج: مخزن موزّع (Redis) بدل الذاكرة فقط إن تعددت النسخ.

**ما تم في Fanni**
- Login: حدود IP + معرّف (`Too many login attempts…`) — لُوحظ أثناء E2E.
- OTP send/verify، forgot/reset، register، upload: حدود في `auth.ts` / `rateLimit.ts`.

**متبقي**
- التأكد أن `checkRateLimit` في الإنتاج مشترك بين عمليات PM2 (إن لزم Redis).
- تنبيهات Sentry عند ارتفاع 429.

---

## 5) ما تم في طبقة الاختبارات (Playwright / Logic Suite)

| المحور من التقرير | تغطية E2E الحالية |
|-------------------|-------------------|
| خصم/قبول ذري (تعارض قبول مزدوج) | ✅ `30-edge` B |
| شحن تأكيد/رفض | ✅ `10-wallet` A/B |
| بونص أدمن / ترحيب (ناعم) | ✅ `10-wallet` C/D |
| إنشاء/إلغاء/رفض/إكمال/تقييم | ✅ `20-order-lifecycle` |
| عدم وصول / حلول بديلة / استرداد | ✅ `20` + `40` |
| إلغاء بعد قبول مرفوض | ✅ `30-edge` C |
| fail قبل start / إكمال مبكر | ✅ `30-edge` D/E |
| قناع البيانات قبل القبول | ✅ `30-edge` F (جزئي حسب شكل الاستجابة) |
| سباق متزامن N عمال | ❌ لم يُنفَّذ بعد |
| مرجع دفع مكرر | ❌ لم يُنفَّذ بعد |
| PII في وصف المشكلة | ❌ لم يُنفَّذ بعد |
| GPS spoof / سقف نصف القطر | ❌ لم يُنفَّذ بعد |
| فرض mustChangePassword على API | ❌ جزئي (UI فقط) |

تشغيل موصى به (تصوير واجهة حقيقي):

```bat
set E2E_USE_LOCAL=1
set E2E_LOGIC_UI_LOGIN=1
scripts\run-e2e-logic-suite.cmd
```

---

## 6) خارطة تنفيذ موصى بها (حسب الأثر)

### P0 — أسبوع
1. Unique + تحقق على `reference_number` لطلبات الشحن.  
2. فلتر PII على وصف الطلب (سيرفر).  
3. Middleware يمنع أدمن بـ `mustChangePassword` من التأكيدات المالية.  

### P1 — أسبوعان
4. اختبار تزامن قبول الطلب (Vitest/k6).  
5. سقف تشغيل رسمي لـ `BROADCAST_RADIUS_TIERS_KM` في env الإنتاج.  
6. إرفاق إيصال إلزامي للشحن اليدوي.  

### P2 — لاحقًا
7. بوابة دفع آلية.  
8. كشف سلوك تهرّب العمولة.  
9. مراجعة قانونية لتصنيف النقاط.

---

## 7) إقرار

- كثير من ثغرات التقرير **مغطاة تصميميًا في الكود** (خصوصًا سباق القبول، الإخفاء، النزاعات، rate limit، شروط النقاط).  
- الفجوات الأخطر المتبقية للتشغيل والمال: **مرجع التحويل المكرر**، **PII في الوصف**، **فرض تغيير كلمة أدمن على مستوى API**.  
- طبقة E2E المنطقية الحالية تغطي دورة المحفظة/الطلب الأساسية؛ تحتاج توسيعًا للحالات الأمنية أعلاه.

**إعداد التقرير:** استنادًا إلى مراجعة المستودع `E:\UpNexa.com\fanni` بتاريخ 5 سبتمبر 2026 ومصفوفة الثغرات المرفقة من المستخدم.
