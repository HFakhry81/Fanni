# Fanni — تقرير حلول سد الثغرات والفجوات الهندسية

**التاريخ:** 5 سبتمبر 2026 (محدَّث بعد التنفيذ)  
**المرجع:** تحليل الثغرات + وثائق الموديولات + حالة الكود في `main`  
**الهدف:** أفضل الحلول، وما تم تنفيذه فعليًا.

---

## 0) الخلاصة التنفيذية

| المجال | الحالة | ملاحظة التنفيذ |
|--------|--------|----------------|
| سباق قبول الطلب / خصم النقاط | **مسدود** | `FOR UPDATE` + E2E قبول متزامن |
| شحن يدوي / مرجع مكرر | **مسدود** | Unique index + تحقق API |
| إيصال التحويل إلزامي | **مسدود** | `proof_image_url` + واجهة فني/أدمن |
| تصنيف النقاط (ليست نقدًا) | **موجود** | شروط الاستخدام |
| تسريب هاتف في الوصف | **مسدود** | رفض سيرفر `CONTACT_PII_IN_DESCRIPTION` |
| GPS spoofing | **مسدود جزئيًا** | رفض قفزات غير فيزيائية + سقف بث |
| توسع الدائرة | **مسدود** | `BROADCAST_MAX_RADIUS_KM` (افتراضي 100) |
| نزاع / عميل غير مستجيب | **موجود** | fail-service + استرداد مشروط |
| تهرّب العمولة بعد الكشف | **موجود** | فني لا يلغي؛ استرداد مربوط بالاتصال |
| أدمن افتراضي | **مسدود** | `MUST_CHANGE_PASSWORD` على API |
| Rate limit Login/OTP | **موجود** | كما سبق |

---

## 1) ما تم تنفيذه في هذا التغيير

### 1.1 المدفوعات والمحفظة
- Migration `028_payment_proof_and_ref_unique.sql`: عمود `proof_image_url` + فهرس فريد على المرجع.
- `POST /payments/request` يفرض: `packageId`، مرجع صالح، إيصال تحت `uploads/{userId}/`.
- رفض 409 عند تكرار المرجع (`REFERENCE_DUPLICATE`).
- واجهة الفني: حقول المرجع + رفع الإيصال؛ الأدمن يعرض الإيصال.

### 1.2 تسريب التواصل
- `orderTextContainsContactPii` → رفض إنشاء الطلب برمز `CONTACT_PII_IN_DESCRIPTION`.
- الإبقاء على التعقيم كطبقة ثانية.

### 1.3 أدمن / كلمة المرور
- `requireAdmin` يمنع كل مسارات الأدمن إن `mustChangePassword=true` عدا:
  - `/admin/my-permissions`
  - `/admin/dashboard/stats`
- الاستجابة: `403` + `code: MUST_CHANGE_PASSWORD`.

### 1.4 الجغرافيا والبث
- `broadcastRadius.ts`: سقف أقصى (افتراضي 100 كم، سقف مطلق 250).
- `geoJump.ts` + `POST /geo/update`: رفض قفزات GPS غير منطقية.

### 1.5 الاختبارات
- Vitest: sanitizer، payment validation، broadcast/geo، requireAdmin.
- Playwright: `e2e/tests/logic/50-security-gaps.spec.ts` (PII، مرجع مكرر، قبول متزامن).
- تحديث مسارات الشحن في logic/full-app لرفع إيصال.

---

## 2) تشغيل مطلوب بعد النشر

```bat
pnpm --filter @workspace/db migrate
```

Env اختياري:
```text
BROADCAST_RADIUS_TIERS_KM=15,50,100
BROADCAST_MAX_RADIUS_KM=100
```

---

## 3) متبقي خارج نطاق هذا التنفيذ (P2)

- بوابة دفع آلية (OPay/…).
- كشف سلوكي متقدم لتهرّب العمولة خارج المنصة.
- مراجعة قانونية رسمية لتصنيف النقاط أمام الجهات التنظيمية.
- Redis لـ rate limit عبر عدة نسخ PM2 (إن لزم).

---

**إعداد التقرير:** تنفيذ 5 سبتمبر 2026 على المستودع المحلي.
