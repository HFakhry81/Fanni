# Database
DATABASE_URL=postgresql://user:pass@localhost:5432/fanni

# Session / JWT signing (required — no default)
SESSION_SECRET=change-me-to-a-long-random-secret

# OTP phone verification
# false (default): registration is accepted without a valid OTP token (development / testing)
# true            : a verified OTP token is required to complete registration or admin creation
ENABLE_OTP=false

# SMS gateway (optional — when absent, OTP codes are logged to the console)
# SMS_API_KEY=your_sms_api_key

# Comma-separated browser origins allowed with credentials. Native apps send no Origin.
# CORS_ORIGINS=https://api.upnexa-eg.com,https://app.upnexa-eg.com
# DISPUTE_AUTO_DAILY_CAP=2
# PUBLIC_API_URL=https://api.upnexa-eg.com
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_VOICE_NUMBER=
# STORAGE_DRIVER=local
# PRIVATE_OBJECT_DIR=/var/www/storage/fanni
# PRIVATE_OBJECT_DIR_ID=/var/www/storage/fanni/id
# PRIVATE_OBJECT_DIR_CARNEHAT=/var/www/storage/fanni/carnehat
