{
  "name": "@workspace/e2e",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:local": "playwright test --project=local-chrome",
    "test:lt": "playwright test --project=lambdatest-chrome",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.1",
    "dotenv": "^16.4.7"
  }
}
