# LambdaTest — copy to repo root .env or e2e/.env (never commit real keys)
LT_USERNAME=your_lambdatest_username
LT_ACCESS_KEY=your_lambdatest_access_key

# Optional
LT_BUILD=Fanni E2E Build
LT_PLATFORM=Windows 11
E2E_BASE_URL=https://app.upnexa-eg.com
E2E_API_URL=https://api.upnexa-eg.com

# Optional — for authenticated flows (not used by smoke tests yet)
# E2E_ADMIN_IDENTIFIER=
# E2E_ADMIN_PASSWORD=
# E2E_CLIENT_IDENTIFIER=
# E2E_CLIENT_PASSWORD=
