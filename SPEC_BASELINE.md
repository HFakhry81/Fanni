# Fanni — القائمة الموحّدة للمواصفات (APPROVED BASELINE)

آخر اعتماد: 26 أغسطس 2026

هذه القائمة تجمع مواصفاتك النهائية (7 وحدات) مع شروط المشروع الحالية وأفضل ممارسات منصات الـ Lead. أي تنفيذ يخالفها يُسجَّل **فجوة** أو **انحراف**. لا تُحذف الصفوف التاريخية من `PROJECT_DEVELOPMENT_REVIEW.md`؛ تُحدَّث هذه القائمة بالإضافة عند تغيير قرار معتمد.

المصادر: وحدات التسجيل، الطلبات، العنونة، محفظة النقاط، الفواتير/النقاط، الحسابات ودفتر الأستاذ، الأدمن — مع سياسة OCR/الاتصال المقنّع الحالية.

---

## قرارات حُسمت (تعارضات سابقة)

| الموضوع | قيم قديمة في التنفيذ | القيمة المعتمدة الآن | السبب |
|---|---|---|---|
| هدية الترحيب | 50 نقطة | **60 نقطة مرة واحدة** | مواصفة المحفظة + دفتر الأستاذ |
| باقات الشحن | 50/100/200 بـ 1:1 | **100→120، 250→300، 500→600، 750→900، 1000→1200** (120 نقطة لكل 100ج؛ مواصفة Word النهائية) | معتمد من نظام المحفظة ودفتر الأستاذ النسخة النهائية |
| سعر النقطة | 1 نقطة = 1 جنيه | النقاط **أرصدة استخدام**؛ النسبة 120/100 | فصل النقد عن التشغيل + التزامات CBE |
| فتح Lead | 15 ثم 20 | **20 افتراضي من قاعدة البيانات** | لا hardcode في التطبيق |
| OCR فواتير شراء | كان مطلوبًا ثم أُزيل | **مُزال بالكامل**؛ لا مواد يدوية ولا فواتير ثلاثية للصيانة | قرار 26 أغسطس: محاسبة = عمولة Lead فقط |
| واتساب مباشر | أزرار tel/واتساب | **اتصال مقنّع**؛ الرقم لا يُعرض للطرفين | خصوصية + تتبع |
| بوابة الدفع | تحويل يدوي الآن / OPay لاحقًا | **يدوي الآن**؛ OPay/Paymob **قابل للضبط** دون أرقام ثابتة | لا hardcode لرسوم البوابة |
| الأدمن | تبويبات متفرقة | **7 وحدات IA**: Dashboard, Operations, Users, Catalog, Finance, Analytics, Administration | حوكمة تشغيلية |

---

## شروط الاستخدام المعتمدة (اقتصاد النقاط)

التفاصيل القانونية/التشغيلية في `TERMS_OF_USE.md`:

- النقاط أرصدة استخدام رقمية **غير قابلة للسحب النقدي** ولا تُحوَّل بين فنيين.
- الكشف بعد التأكيد **غير قابل للاسترداد** إلا نزاع/سياسة/أدمن.
- الإلغاء من العميل مسموح **قبل** كشف البيانات.
- الاسترداد الآلي لاحقًا: رقم خاطئ / عدم استجابة + حد يومي + أثر نقر الاتصال.

---

## الوحدات والمطلوب المعتمد

### 0 — الهوية والصلاحيات والحوكمة

- فصل Authentication عن Profile (عميل / فني / أدمن).
- جلسة تطبيق المستخدم **لا** تفتح مسارات الأدمن حتى لو `users.role = admin`.
- كل `/admin/*` يمرّ `requireAdmin` (جلسة أدمن + سجل نشط في `admins`).
- المال يحتاج صلاحية صريحة: `manage_wallet` / `manage_payments` / `manage_disputes` / `manage_pricing` / `manage_expenses`.
- Least privilege + فصل الواجبات + Audit (من/ماذا/متى/قبل/بعد/سبب/جلسة).
- CORS لأصول معروفة فقط مع cookies.
- حد معدّل على OTP والدخول والرفع.
- أدوار: Super Admin، Operations، Verification، Support، Finance، Catalog، Auditor.

### 1 — التسجيل والاعتماد (Registration)

**عميل:** بيانات → تفضيل دفع (ليس معاملة) → موقع أساسي PostGIS → OTP (صلاحية، محاولات، عدم إعادة استخدام) → ACTIVE.

**فني:** OTP ≠ اعتماد مهني. حالات التحقق: NOT_SUBMITTED → SUBMITTED → UNDER_REVIEW → APPROVED / REJECTED / NEEDS_CORRECTION. بطاقة وجه وظهر على **مخزن خاص**. ساعات العمل ≠ Availability. منطقة الخدمة ≠ المسافة. لا يستقبل طلبات قبل ACTIVE. المحفظة تُنشأ عند الاعتماد مع هدية 60 مرة واحدة مربوطة بالهوية.

### 2 — العنونة والجغرافيا (Module 06)

- Address (محافظة/حي/شارع/مبنى…) **ليس** Geolocation (lat/lon/accuracy/timestamp/source).
- محافظات ومناطق من جداول قابلة للإدارة لا نص حر.
- تأكيد الموقع بعد الخريطة؛ Reverse geocoding قابل للتعديل.
- PostGIS نقطة حقيقية؛ تتبع أثناء `en_route`؛ جيوفنس وصول؛ بيانات للنزاع والتدقيق.

### 3 — الطلبات والمطابقة

دورة معتمدة:

`PENDING → MATCHING → OFFERED → (رفض بلا خصم → MATCHING) | (قبول → تأكيد نقاط → خصم ذري → كشف) → ACCEPTED → GPS → وصول → نعم=IN_PROGRESS / لا=مؤقت 30د ثم إسقاط → نتيجة خدمة → تقييم → CLOSED`

- قبل الكشف: نوع الخدمة، منطقة عامة، مسافة تقريبية، موعد، وصف منظَّف، تكلفة النقاط. بلا هاتف/عنوان دقيق.
- لغة التأكيد مصرية: «قبل ما نكمّل» / «موافق وكمل» / «لا، مش دلوقتي».
- الخصم والكشف على السيرفر داخل معاملة واحدة + دفتر.
- تسعير: خدمة + يوم + وقت (+ نوع/منطقة لاحقًا) بأولوية: قاعدة أدق ثم تخصص ثم الافتراضي 20.
- تقييم يبقى على الطلب المقفل حتى يملأه العميل.

### 4 — محفظة النقاط

- رصيد تشغيلي منفصل عن دفتر الأستاذ.
- نوعان داخليًا: Promotional vs Purchased (العرض للمستخدم رصيد واحد).
- نية شحن `Recharge Intent` قبل الدفع؛ النقاط بعد تأكيد السيرفر فقط؛ Idempotency.
- حالات الشحن: INITIATED → PAYMENT_PENDING → PAID / FAILED / CANCELLED / EXPIRED / REFUNDED.
- تعديل رصيد أدمن: سبب إلزامي + هوية + قبل/بعد.

### 5 — التتبع والوصول ونتيجة الخدمة

- جيوفنس على `en_route`؛ سؤال العميل نعم/لا؛ مؤقت 30 دقيقة ثم إسقاط وإعادة مطابقة.
- نتيجة الخدمة: نعم → إكمال+تقييم؛ لا → أسباب إلزامية. استرداد استثنائي سياسة لا خصم من التطبيق.

### 6 — الفواتير والمحاسبة (Modules 07 + فواتير)

- **قرار 26 أغسطس:** لا فواتير مشتريات/OCR ولا محاسبة تفاصيل الصيانة (أجور/مواد/نقل). إيراد المنصة = عمولة كشف Lead (نقاط) + GL للمحفظة/الشحن.
- جداول الفواتير التاريخية تبقى للقراءة فقط؛ الكتابة عبر API متوقفة (410).
- Wallet = sub-ledger تشغيلي. GL = قيود مزدوجة (مدين=دائن) لأحداث النقاط/الشحن/الاسترداد/المصروفات التشغيلية.
- الشحن نقدًا → التزام نقاط (Deferred Revenue) حتى الاستهلاك → Lead Access Revenue.
- الهدية 60 = تكلفة ترويج لا إيراد.
- دليل حسابات + مراكز تكلفة + فترات محاسبية + عكس القيد بدل الحذف (GL المتقدم مؤجل جزئيًا).
- رسوم البوابة وSMS/Email من إعداد Super Admin بتاريخ سريان (OPay مؤجل).
- الآن: تحويل بنكي يدوي + تأكيد أدمن. لاحقًا: OPay مع webhook وتحقق وتسوية.

### 7 — الاتصال والإشعارات

- إخفاء الأرقام عن العميل والفني. اتصال عبر Twilio Voice. الأدمن قد يرى الرقم الكامل.
- تنظيف وصف الطلب من هاتف/بريد/واتساب/تيليجرام.

### 8 — الإدارة (Module 08)

سبعة أقسام: لوحة، عمليات، أطراف، كتالوج، مالية، تحليلات، إدارة. لا قائمة جداول تقنية. إعدادات مركزية وfeature flags. سجل دخول + Audit.

### 9 — التخزين والمراقبة والاختبار

- مخزن خاص لـ KYC الفني (جودة منخفضة، VPS أو object خاص، مدة احتفاظ ثم حذف).
- مخزن منفصل لصور المشكلة/مراحل العمل.
- لا `makePublic` على بطاقة الهوية.
- Sentry قائم. اختبارات تدفق عميل/فني/أدمن قبل اعتبار الوحدة مغلقة.

---

## مصفوفة الفجوات والانحرافات (مقابل الكود الحالي)

| الوحدة | الحالة | فجوة / انحراف | أولوية |
|---|---|---|---|
| 0 حوكمة | شبه مغلق | RBAC + CORS + حد معدل + تدقيق مالي + شاشة audit-logs. أدوار يومية كاملة اختيارية | P2 |
| 1 تسجيل | كود جاهز | اعتماد فني + ترحيب 60؛ أخطاء auth واضحة؛ PostGIS اختياري عند التسجيل؛ KYC ينتظر `.env` VPS | P0 |
| 2 جغرافيا | شبه مكتمل | Accuracy/Source على `/geo/update`؛ تأكيد موقع موحّد اختياري | P2 |
| 3 طلبات | شبه مكتمل | إخفاء ثم كشف بعد الخصم موجود؛ ينقص اختبار e2e كامل | P1 |
| 4 محفظة | محاذاة الأرقام | باقات Word 100/250/500/750/1000 وهدية 60؛ فصل Promotional/Purchased في 018؛ محاذاة 022 | P2 |
| 5 تتبع | تم في الكود | تحقق تشغيلي حي | P2 |
| 6 محاسبة | شريحة أولى (جزئي) | محرك قيد مزدوج + ترحيل + ميزان/قيود في الأدمن. ينقص: فترات محاسبية، مراكز تكلفة، تدفق نقدي، تسوية بوابة، واجهة ضبط نسبة الرسوم (المفتاح موجود وقيمته 0) | P2 |
| 7 اتصال | مؤجّل | Twilio غير متاح؛ الاتصال المقنّع 503 حتى الحساب | P2 |
| 8 أدمن | 7 تبويبات | التفاصيل (مدفوعات/نزاعات/أستاذ) من اللوحة | P2 |
| 9 تخزين/اختبار | جاهز للرفع | خطوات: `deploy/VPS-STEPS.md`؛ CORS إنتاجي في الكود؛ KYC عند تشغيل السكربت | P0 |

ترتيب السد: **0 → 4 اقتصاد الباقات/الهدية → 1 KYC → 9 تخزين → 6 شاشات مالية → 3 اختبار تدفق → 7 تشغيل الاتصال → 8 IA الأدمن → 2/5 ضبط**.

---

## بوابة قبول سريعة قبل النشر

- [x] جلسة المستخدم لا تمر على `/admin/*`.
- [x] صلاحيات المال مربوطة في الـ API.
- [x] الباقات والهدية في migrations 017/018/022 (سلم Word)؛ دفتر الأستاذ 020 طُبّق على التطوير المحلي.
- [x] صور الهوية غير عامة على مسار الرفع الخاص.
- [x] شروط الاستخدام معروضة في التسجيل.
- [x] كود KYC الخاص جاهز (مجلدات id/carnehat؛ ليس عليماً).
- [ ] تشغيل `deploy/VPS-STEPS.md` على سيرفر الأصل.

---

## تقرير نواقص المسار الكامل (E2E) — 22 أغسطس 2026

المسار المعتمد: تسجيل → اعتماد فني (60 نقطة) → شحن يدوي → طلب عميل → إشعار مخفي → موافق وكمل → تتبع/جيوفنس → نتيجة خدمة. المحاسبة = عمولة فتح الطلب فقط (بدون فواتير صيانة). الاتصال المقنّع مؤجّل.

| خطوة E2E | الحالة | الناقص |
|---|---|---|
| تسجيل عميل + OTP | موجود | — (موافقة الشروط في آخر خطوة تسجيل) |
| تسجيل فني + اعتماد | موجود | رفع البطاقة إلى `id/` والكارنيه إلى `carnehat/`؛ تطبيق مجلدات الـ VPS في بيئة السيرفر |
| ترحيب 60 | كود + 017/020 على التطوير المحلي | — |
| شحن يدوي (نية طلب دفع) | موجود | OPay مؤجّل حتى وصول الـ API |
| إنشاء طلب + تطهير وصف | موجود | — |
| إشعار/قائمة مخفية | موجود (`maskSensitiveOrderFields`) | اختبار أن الإشعار لا يحمل رقمًا |
| تأكيد الخصم والتعريفة | موجود | — |
| اتصال مقنّع | مؤجّل | Twilio غير متاح الآن؛ المسار الحي بدون اتصال مقنّع |
| جيوفنس + 30 دقيقة + نتيجة | كود | تجربة جهاز حي (المسار مؤكد من المشغّل؛ لقطة عند الفشل) |
| استرداد استثنائي | نزاع/أدمن + آلي | حد يومي افتراضي **2** (`DISPUTE_AUTO_DAILY_CAP`) |
| عمولة Lead (بدون فواتير صيانة) | مُنفَّذ — محاسبة عمولة فقط | شريحة GL للمحفظة؛ ينقص فترات/مراكز تكلفة وتسوية بوابة |
| صور | محول خاص | `PRIVATE_OBJECT_DIR=/var/www/storage/fanni` مع `.../id` و`.../carnehat` |

لا يُربط OPay ولا يُطلب باسورد الـ VPS في هذه المرحلة.

---

## خطة E2E 360° — اعتماد 26 أغسطس 2026

مسار قبول كامل عبر الأدوار الثلاثة قبل بناء APK التالي. الحالة = **كود جاهز** ما لم يُذكر خلاف ذلك.

### أ) عميل (Client)
| # | الخطوة | الحالة | ملاحظة |
|---|---|---|---|
| C1 | تسجيل + شروط نقاط | كود جاهز | أخطاء السيرفر تظهر نصًا حقيقيًا (ليس «تعذّر الاتصال» الزائف) |
| C2 | دخول بكلمة مرور | كود جاهز | `getApiBase()` → `https://api.upnexa-eg.com` |
| C3 | إنشاء طلب + صور + خريطة | كود جاهز | MapPicker: فصل JS/native drivers |
| C4 | متابعة الطلب / إلغاء | كود جاهز | |
| C5 | تتبع حي + مشاركة | كود جاهز | WS عبر `getWsUrl()` |
| C6 | تأكيد وصول / نتيجة خدمة | كود جاهز | تحقق جهاز حي |

### ب) فني (Technician)
| # | الخطوة | الحالة | ملاحظة |
|---|---|---|---|
| T1 | تسجيل + وثائق KYC | كود جاهز | ينتظر `.env` KYC على VPS |
| T2 | اعتماد أدمن → هدية 60 | كود جاهز | migrate 017/022 على VPS |
| T3 | شحن يدوي / باقات Word | كود جاهز | OPay مؤجّل |
| T4 | استلام Lead مخفي → فتح بالنقاط | كود جاهز | |
| T5 | توافر / خريطة / طلبات | كود جاهز | |
| T6 | وصول + إكمال | كود جاهز | |

### ج) مسئول (Admin)
| # | الخطوة | الحالة | ملاحظة |
|---|---|---|---|
| A1 | دخول + لوحة 7 تبويبات | كود جاهز | ملف شخصي / تسجيل خروج موصولان |
| A2 | اعتماد فنيين / مستخدمين | كود جاهز | |
| A3 | مدفوعات / نزاعات / تسعير Lead | كود جاهز | |
| A4 | أستاذ / مصروفات / تدقيق | كود جاهز | GL شريحة أولى |
| A5 | خريطة حية + إعادة تحديد مواقع | كود جاهز | إصلاح Base URL |

### د) البنية والقبول
| # | البند | الحالة |
|---|---|---|
| I1 | API `healthz` من الهاتف | مُؤكد (متصفح الجهاز) |
| I2 | نشر commits الاتصال/الأنيميشن على VPS | **مطلوب قبل إعادة الاختبار** |
| I3 | PostGIS على إنتاج | مُستحسَن؛ التسجيل يتخطى geography إن فشل |
| I4 | بناء APK جديد | **منتهٍ** v1.0.6 (`c9c57cc3`) — ارفع إلى `app.upnexa-eg.com/fanni.apk` |
| I5 | Twilio / OPay | مؤجّل — لا يوقف E2E الأساسي |

**تعريف «E2E 360 مكتمل»:** C1–C6 + T2–T6 + A1–A4 على جهاز حي ضد `api.upnexa-eg.com` بعد نشر الـ API، بدون Twilio/OPay.

---

## جرد TODO — حوكمة ونظافة (22 أغسطس 2026)

**24 بندًا:** 18 منتهية في الكود، 1 شريحة أولى (دفتر الأستاذ)، 5 متبقية حيّة (بيئة/أسرار + KYC).

المنتهي: RBAC، CORS، حد المعدّل، تدقيق مالي API، اقتصاد 60/20 وباقات Word (022)، أوعية النقاط، شحن يدوي، دورة Lead المخفية، جيوفنس، اتصال مقنّع في الكود، شروط التسجيل، رفع خاص، محاسبة عمولة فقط (بدون OCR/فواتير صيانة)، شاشة تدقيق، نزاع آلي، Accuracy/Source، migrate 017–022، هيكل IA سبعة تبويبات.

شريحة أولى (جزئي): قيد مزدوج — جداول 020 (مطبّقة محليًا)، ترحيل من الشحن/الترحيب/الفتح/الاسترداد/المصروف، ميزان مراجعة وقائمة قيود. ليس دفترًا محاسبيًا كاملًا (لا فترات/مراكز تكلفة/تسوية بوابة).

المتبقي الحي:
1. تشغيل `deploy/VPS-STEPS.md` على الأصل (git pull + `.env` + `deploy-vps.sh`)
2. e2e حي بدون اتصال مقنّع
3. Twilio لاحقاً (لا يوقف هذا الرفع)
4. OPay عند وصول الـ API

إكمال GL لاحقًا (غير حاجز للنشر بعد P0): فترات، مراكز تكلفة، تدفق نقدي، واجهة نسبة الرسوم.

آخر دفع معروف: راجع `git log -1` على `main`. طُبّق `020_general_ledger.sql` على التطوير المحلي.
