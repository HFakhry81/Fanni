# مراجعة دورة OCR والفواتير في Fanni

آخر مراجعة: 26 أغسطس 2026

## القرار المعتمد (محدث)

1. **إزالة تامة** لرفع فواتير المشتريات وOCR من التطبيق والخادم.
2. **المحاسبة = عمولة فقط** (كشف Lead / اقتصاد النقاط). المنصة **لا** تتدخل في تفاصيل أجور/مواد/نقل أعمال الصيانة.
3. إكمال الطلب لا يُنشئ فواتير ثلاثية ولا يطلب `labourFee` / `materialsTotal`.
4. جداول/أعمدة `invoices` و`ocr_*` و`materials_photos` تبقى في قاعدة البيانات للقراءة التاريخية فقط — **لا حذف بيانات إنتاج** الآن.
5. مسارات الكتابة `POST /api/invoices` و`PATCH .../pay|cancel` تُرجع **410** (`JOB_INVOICES_RETIRED`).
6. واجهات العميل/الفني لفواتير الصيانة مخفية أو رسالة اعتزال؛ المحفظة والنقاط تبقى.

## الدورة الحالية لإغلاق الطلب

1. الفني يفتح نموذج الإكمال (وصف الحل / رضا العميل / صور بعد — اختياري).
2. `PATCH /api/orders/:id/complete` يحدّث الحالة إلى `completed` فقط.
3. لا فواتير، لا بريد فواتير، لا OCR.

## بوابة القرار

- [x] إيقاف واجهة OCR ورفع فواتير المشتريات.
- [x] إيقاف endpoint `/api/ocr/receipt` (سابقًا).
- [x] إيقاف إنشاء الفواتير الثلاث عند الإكمال.
- [x] إيقاف إنشاء/دفع/إلغاء فواتير الوظائف عبر API (410).
- [x] الإبقاء على أعمدة/جداول OCR والفواتير التاريخية دون DROP.
- [x] المحاسبة التشغيلية للمنصة = عمولة Lead + محفظة نقاط (+ مصروفات تشغيلية/GL نقاط حسب المواصفة).
